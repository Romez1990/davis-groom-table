<?xml version="1.0" encoding="utf-8"?>
<metadata>
	<layout title="spec">
	</layout>
	<state>
		<name>idspec</name>
		<url>
			<param name="idspec" type="sql" default="0" label="Выберите специалиста" query="SELECT id AS value, name as idspec FROM #__ttfsp_spec WHERE published=1 ORDER BY ordering" />
		</url>
	</state>
<fields name="request">
<fieldset name="request" label="spec">
			<field name="idspec" type="sql" default="0" label="Выберите специалиста" query="SELECT id AS value, name as idspec FROM #__ttfsp_spec WHERE published=1 ORDER BY ordering" />
</fieldset>
</fields>
</metadata>