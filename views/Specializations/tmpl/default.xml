<?xml version="1.0" encoding="utf-8"?>
<metadata>
	<layout title="Specializations">
	</layout>
	<state>
		<name>Specializations</name>
		<url>
			<param name="sid" type="sql" default="0" label="Выберите специализацию" query="SELECT id AS value, name as sid FROM #__ttfsp_sprspec  WHERE published=1 ORDER BY ordering" />
		</url>
	</state>
<fields name="request">
<fieldset name="request" label="Specializations">
			<field name="sid" type="sql" default="0" label="Выберите специализацию" query="SELECT id AS value, name as sid FROM #__ttfsp_sprspec  WHERE published=1 ORDER BY ordering" />
</fieldset>
</fields>
</metadata>